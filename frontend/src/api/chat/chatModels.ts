export interface IChatListElement {
    id: string;
    isGroup: boolean;
    title: string;
}

export interface IChatDetails {
    id: string;
    isGroup: boolean;
    title: string;
    info: string;
}
